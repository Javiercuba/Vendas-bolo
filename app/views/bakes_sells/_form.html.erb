<%= form_with(model: bakes_sell) do |form| %>
  <% if bakes_sell.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bakes_sell.errors.count, "error") %> prohibited this bakes_sell from being saved:</h2>

      <ul>
        <% bakes_sell.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label( :name,"Bolo") %>
    <%= form.text_field :name, class: "field" %>
  </div>

  <div>
    <%= form.label( :price,"Preço") %>
    <%= form.number_field :price, class: "field",in: 1.0..20.0,step:0.1, autocomplete: 'off' %>
  </div>

  <div>
    <%= form.label( :date_sell,"Data de venda") %>
    <%= form.text_field :date_sell, class: "field", autocomplete: 'off' %>
  </div>

  <div>
    <%= form.label( :quantity,"Quantidade") %>
    <%= form.number_field( :quantity, in: 1..100, step:1,class: "field", autocomplete: 'off') %>
  </div>

  <div>
    <%= form.submit("Cadastrar venda",:class =>"button-43") %>
  </div>
<% end %>
